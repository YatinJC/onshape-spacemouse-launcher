#!/bin/bash
# Launch spacenav-ws server and open Onshape in the default browser

# Start server only if not already running
if ! pgrep -f 'spacenav-ws.*serve' >/dev/null; then
    uvx spacenav-ws@latest serve &
    sleep 2
fi

xdg-open "https://cad.onshape.com"
